One-sentence: the site footer — carvão background, verde-garrafa hairline separating it from the page, social links as icons only.

```jsx
<SiteFooter />
<SiteFooter compact />
```

Never label the social icons with text. Keep the top hairline: it is what separates the footer from the vinho/carvão gradient above.
